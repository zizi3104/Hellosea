# HELLO SEA 관리자 연결 안내

## 현재 구현

- 고객용 페이지와 관리자 페이지(`/admin`) 완전 분리. 메인에 편집 링크, 업로드 폼, 관리자 스크립트를 넣지 않습니다.
- 이메일 회원가입 / 이메일 인증 후 로그인 / 로그아웃.
- 회원가입만으로 관리자 권한이 생기지 않습니다. 이메일 인증을 마치고 서버의 `ADMIN_USER_IDS`에 지정된 계정만 콘텐츠를 수정합니다. 다른 회원은 로그인 후 승인 대기 안내만 볼 수 있습니다.
- AYO SURF 학교 소개, 두 사람의 이름·사진·영어/한국어/일본어 소개, 예시 표시를 편집합니다. 전체 사이트 CMS나 문의 관리 화면은 아닙니다.
- 사진은 JPG/PNG/WebP, 장당 1MB 이하. 이미지 데이터는 소개 내용과 함께 DB에 저장합니다.
- ‘저장 및 게시’가 성공한 후 사이트를 다시 열면 최신 내용을 표시합니다. 동시 수정은 버전 검사로 덮어쓰기를 방지합니다.
- 토큰은 HttpOnly/SameSite 쿠키에 저장하고 운영 HTTPS에서는 Secure를 적용합니다. 비밀번호·인증 토큰은 localStorage에 저장하지 않습니다. 세션은 최대 1시간 후 다시 로그인합니다.

## 연결 순서

1. 기존 Supabase 프로젝트에서 `202609210002_team_content.sql` 마이그레이션을 한 번 적용합니다. 새 프로젝트라면 문의용 첫 마이그레이션도 먼저 적용합니다.
2. Supabase Authentication에서 이메일 로그인과 이메일 인증을 켭니다. Site URL을 실제 HTTPS 사이트 주소로 설정하고 인증 후 관리자 로그인 페이지로 돌아오도록 Redirect URL에 `https://hellosea.shop/admin`을 추가합니다. 운영 이메일 발송을 위한 SMTP를 설정합니다.
3. Vercel에 기존 `SUPABASE_URL`, `SUPABASE_SECRET_KEY`, `ALLOWED_ORIGINS`와 새 `SUPABASE_PUBLISHABLE_KEY`를 설정합니다. 실제 비밀키는 소스에 넣지 않습니다.
4. 사이트에 소스를 배포한 뒤 `/admin`에서 본인 계정을 가입하고 이메일을 인증합니다. 이메일 링크의 토큰은 관리자 페이지에서 저장하지 않으므로 인증 후 이메일과 비밀번호로 다시 로그인합니다.
5. Supabase Authentication Users에서 승인할 본인의 **사용자 UUID**를 확인합니다. Vercel의 `ADMIN_USER_IDS`에 해당 UUID를 설정하고 재배포합니다. 여러 명은 쉼표로 구분합니다. 이 값은 고객 입력이나 사용자 프로필 메타데이터에서 받지 않습니다.
6. 승인 계정으로 로그인해 사진과 소개를 저장합니다. 새 고객용 브라우저에서 게시 결과를 확인합니다. 일반 가입자와 로그아웃 상태에서 저장 요청이 거부되는지도 확인합니다.

## 확인 범위와 제한

코드, 모의 API 테스트, 정적 빌드는 검증했습니다. 현재 실제 Supabase/Vercel 계정이 연결되지 않아 회원가입 메일 발송, 실제 로그인·DB 저장·운영 배포는 수행하지 않았습니다. 별도 관리자 HTML 미리보기는 로그인 화면 확인용이며 실제 인증과 저장을 수행하지 않습니다. 고객 preview에서는 관리자 기능을 실행하지 않습니다.

저장 전 편집 내용은 브라우저 메모리에만 있습니다. 페이지를 닫으면 저장하지 않은 내용은 사라집니다. 사진·소개가 포함된 게시된 내용은 공개 정보로 취급됩니다. 비밀번호 재설정 화면은 이번 범위에 포함되지 않았으며 필요 시 Supabase의 계정 관리 절차를 이용합니다.

공식 참고: https://supabase.com/docs/guides/auth/passwords
